# Archive — retired-with-reason, not pending

These four files were superseded by `ebr-app.html` (the unified SPA) during the 2026-09-01 rebuild.
Kept for provenance and diffing, not because they're waiting to be reintegrated.

- **`build-ebr.html`** — the "idea-chess rework" track (2026-08-27). Its good content (capstone diff
  screen, multi-file/Stefanie-template setup) was folded into `ebr-app.html` during Build Pass 1. Do not
  re-link it as a served file.
- **`console.html`** — an earlier nav exploration. `DESIGN-REFS.md` names it directly: "a franken harness"
  against the real HARNESS product screenshot. `ebr-app.html`'s nav supersedes it correctly. Do not restore
  its nav.
- **`ebr-dashboard.html`**, **`ebr-product.html`** — earlier standalone dashboard/marketing explorations,
  superseded by `ebr-app.html`'s Dashboard tab and `landing.html` respectively.

See `briefs/ebr-coworker-rebuild-2026-09-01.brief.md` ("File layout" section) for the decision record.

Not archived here, and not retired: `role-play-simulator.html` stays a served file, linked from
`ebr-app.html` and labeled "Extended scenario — not MVP" — it is scoped out of the core flow, not cut.
