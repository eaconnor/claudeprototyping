# critic-pass-2 — qbr-prep-sim (post-build)

**Date:** 2026-07-31 · **Reviewed:** `prototypes/qbr-prep-sim/index.html` (1160 lines, full read) against `briefs/qbr-prep-sim.brief.md` (component inventory = build contract) and `critic-pass-1-qbr-prep-sim.md`
**Regime carried:** PROCEED-FLAGGED · Tier 1 Concept · do not claim validation
**Verification basis:** direct read of the built file [CS: VERIFIED for all quoted markup]; interaction behavior cited as [D — verified live 2026-07-31] comes from the orchestrator's browser pass (nav, judge fill, gate block/clear/extract-release, Must 6 DOM check, Must 8 regex, no "under 20 minutes", $ only in rail Eng card, council modal + Esc, mode toggle persisting to `apx-mode-pref`, zero console errors). Code-traced behaviors I did not see executed are tagged [CS: HIGH].

---

## Scorecard + delta

| # | Dimension | Pass 1 | Pass 2 | Δ | Read |
|---|---|---|---|---|---|
| 1 | Coherence | 4 | **5** | +1 | Pass-1's one dock — the Judge step had no surface — is now the loudest element on page 1: prime position, brand-ring emphasis, the exact contracted caption, empty human-only fields, and the [R] "one moment with no tool at all" chip. The premise divergence is on-screen (banner), not just in the rail. Screen now traces to the sourced problem end to end. |
| 2 | Source-fidelity | 4 | **4** | 0 | Tag discipline survived the build: [A]/[R]/[D]/[WOBBLY]/[?]/SEED render in the UI; grep resolutions (tier not in code, baseline_source only) are quoted on-screen with dates; even the CSS tokens carry [CS:] provenance comments. Docked one: the ≈0.4× ratio is an invented number with no SEED stamp (the only one), and "44 voices waiting" in the Hard Mode stub is a specific count with no source in the brief. |
| 3 | Demo-ability | 4 | **4** | 0 | All three ux.md §6 tests are now runnable against real states: hard BLOCKED default for the 5-second test, seeded extract copy for the content audit, working nav + interactions for the walkthrough — verified live, zero console errors. Docked one: clearing BOTH gates leaves the extract asserting "the remaining Unverified item stays held" when none is [CS: HIGH — traced in `clearGate()`, not executed], and gate 2 clears "released" with no extract landing — both are walkthrough hazards on the exact test this build exists to pass. |
| 4 | Differentiation | 4 | **5** | +1 | Pass-1's dock was unevidenced devices with no labels. Now the readiness score is [A]-labeled in the rail, the council modal ships zero invented tensions, and the differentiators are structural on screen: two artifact objects on two pages, hard gate with attribution, judge queue as the centerpiece, defensibility tiers labeled "proposed grammar — not built substrate." This is visibly not an AI report generator. |
| 5 | Risk | 4 | **4** | 0 | Premise laundering named in banner + rail; DIV-004 quarantine built with owner named; market-unsettled carried in rail; echo discipline held everywhere. Docked one: the readiness number escapes its containment (fidelity banner, unlabeled, sits above the client-facing extract page) and the unstamped 0.4× is the most quotable number on the internal surface — both are small F2-shaped leaks. |
| 6 | Scope discipline | 2 | **4** | +2 | The inventory worked. Interactivity matches the contract exactly (nav, one gate clear ×2 rows, one pull expand, one judge fill, modal, mode toggle — everything else painted and labeled so). Connection journey collapsed to badges with the on-screen disclaimer. Docked one: two page-1 elements — the health snapshot and the premise banner — are not in an inventory that rules "anything not listed is OUT." Both are defensible (SD4 seed data; F2 mitigation) but the contract's own rule catches them. |
| | **Total** | **22/30** | **26/30** | **+4** | Ships as a Tier 1 instrument. The three fails below are copy-level, fixable in minutes, and should be closed before the packet leaves the building. |

---

## Punch list

### PASS

- **Judge queue (V10) — the pass-1 coherence FAIL is closed.** Prime position above the pulls, contracted caption verbatim ("the one moment with no tool — this is where you come in"), two empty judgment fields per row, one interactive demo row, two painted rows with disabled buttons honestly titled "painted — one demo row only at Tier 1." Fill works [D — verified live 2026-07-31].
- **8-vs-9 resolved on screen.** Exactly 8 numbered cards ("PULL n / 8 · Vn"), plus a visually distinct dashed V9 card headed "+1 · V9 — NOT one of the 8" carrying "unnumbered by the blueprint · [WOBBLY — single speaker]". U3's ×8 holds in the topbar chip and the section heading.
- **Connection grammar collapsed to badges, and says so.** No config flow, no wizard; on-screen note: "The config/instance/auth journey is NOT exercised at Tier 1; badges only." {G3-17-amended} honored.
- **Pre-connected [A] flag on-screen** on all four first-party cards (N-central ×2, Adlumin ×2, Cove via Pull 5): "pre-connected [A] — assumes N-able MCPs ship authed (open question, J. Scally)." Pass-1 trap 1 closed.
- **Confirm-gate is a hard block, not theater.** Unverified rows render BLOCKED by default with a stamp; clearing writes attribution ("cleared by J. Garlick (operator) · … released to extract under her name"); the cleared row appears in the extract; the second gate stays blocked [D — verified live 2026-07-31]. F3's fail condition (passive warning) does not occur.
- **Must 6 / Must 8 / copy bans hold** [D — verified live 2026-07-31]: internal-note strings absent from the extract DOM; no liability language in the extract; no "under 20 minutes" anywhere; the only $ is the "renders $0" substrate quote inside the rail's Eng card — internal, and it is the fact the ban protects, not a value claim. Extract copy is business-outcome language with no time-saved/percentage/graph framing.
- **Exposure counter present and arithmetically honest:** "This draft documents 6 detected gaps" matches the six register rows; "volume of detection is volume of exposure" rendered with its [R] chip. Pass-1's MISSING conversion closed.
- **Dual-surface is structural.** Three separate page sections, nav note "dual-surface = two artifact objects, never a toggle [R]", page-2 and page-3 subs both restate second-object-not-redaction-toggle. "Toggle" appears only in negation and in the color-mode control. F1 countermeasure built.
- **DIV-004 quarantine built as specified:** bordered "ACP admin surface" frame, page 2 only, caption "this framing never reaches the client surface →" with the divergence, Must 1 ban, and resolution owner (Beth → Nicole, open [?]) all on-screen. Page 3 contains zero instances.
- **Defensibility tier rendered as proposed grammar, with the grep receipt on-screen** ("[D — grep 2026-07-31: zero hits in code; code has baseline_source only]"), and `baseline_source: tenant_configured [D — built, in code]` surfaced in the pull-5 expand. Pass-1 trap 2 closed at the build layer.
- **Council modal ships zero invented tensions** [D — verified live 2026-07-31]: pending slots, "as many as the council names, not a hardcoded count" in the exec summary, synthesis slot literal "[0% Beth — fill this in]", Hard Mode stub disabled, Esc/backdrop close with focus return.
- **Bradley rail complete:** readiness badge + "[A] prototype device — not an evidenced metric"; blurb names the premise as "an instrument for the operator study, *not* a finding, and not evidence of demand"; specific nailed/hurt blocks; all four routed cards with does / why now / if-you-ship-without + ROI blocks that name Unknowns instead of inventing numbers; council entry; attribution slot ("0% Beth — accepted verbatim from pipeline"). Market framed as "opportunity or failed category" per {G2-9/G2-10}.
- **Design system compliant:** `--bg-app`/`--bg-surface`/`--brand`/`--alpha-accent` token block; Inter + JetBrains Mono via Google Fonts; radius tokens throughout; soft blurred shadows only — zero `6px 6px 0` offsets; topbar its own dark surface with 2px brand bottom border in both modes; rail violet with 4px `--alpha-accent` left border + inset glow; 3-way toggle persisting to `apx-mode-pref` [D — verified live 2026-07-31]. Zero Itten leakage: no #D8472B, #C99A2E, Space Mono, or Kalam anywhere [CS: VERIFIED — full-file read].
- **Echo discipline (U5) held everywhere the baseline appears:** fidelity banner ("4–5h self-reported, n=1, echo — not a stat"), time chip (ratio with inline caveat), rail nailed block.
- **Seed hygiene otherwise strong:** all names fictional (J. Garlick, S. Bonk, C. Dibbler — obviously-literary, front-page-test pass), SEED stamps pervasive, license/mapping provenance stated on the health snapshot ("mapping those names onto N-central/Cove/Adlumin territory is our move, not the sources' [A]").

### FAIL

- **FAIL — readiness score escapes the rail.** Contract: "Lives in the **Bradley rail only** (internal), never on page 2/3 content," with the [A]-device label. Built: the fidelity banner carries "Readiness: [critic pass 1: 22/30] · Tier 1" — a second instance, outside the rail, without the [A] label, on a sticky banner that persists above pages 2 and 3 including the client-facing extract. Exactly the placement collision the contract line existed to prevent.
- **FAIL — the ≈0.4× value is unstamped.** Seed rule: "Every number stamped `SEED DATA — invented 2026-07-31`." The time chip's 0.4× is an invented measurement (nothing in the sim produces it) and is the only invented number in the file with no SEED stamp. On the U5 question asked directly: the chip **passes U5's letter** — it is a ratio against the echo-flagged baseline with the caveat inline ("ratio only; baseline is one echo, n=1, not a stat") — but an unstamped invented ratio is still the most quotable number on the internal surface, and it will be read as a demo finding by anyone who screenshots the quarantine frame.
- **FAIL — two unlisted elements on page 1.** The contract rules "Anything not listed is OUT." The client health snapshot (three etop-am-weighted score cards) and the sim-premise banner appear in no inventory row. Both are defensible — the scores are brief seed data {SD4}, the banner operationalizes F2 — but the contract's own exclusion rule catches them. Either amend the inventory or remove them; silence is the only unacceptable option.

### MISSING

- **Two of four contracted badge states never render.** The contract enumerates "auth expired · endpoint down · URL blocked · reauth needed"; the build shows only *reauth needed* (Halo) and *endpoint down* (phish-sim), plus an uncontracted *connection: active* (M365). "Auth expired" and "URL blocked" — both sourced from Will's board {G3-16} — have no surface.
- **S3 (Test Connection preview) has no surface** — no real-data echo line ("we can see N tickets across N queues") anywhere. Contract-consistent (the inventory omits it), but it is a domain-model service with zero rendered counterpart; named per the domain-fidelity check, not charged as a fail.
- **E8 (living documents) is partially surfaced.** Risk register: built. Tech roadmap: present as the extract's "Recommended roadmap" + judge-queue priority field. Tech budget: no surface. The defining property — "carried cycle to cycle" — has no rendered counterpart (no carried-from-last-cycle affordance anywhere), despite the internal notes referencing a third-cycle deferral. E4 (Runbook) and E5 (Value Ledger) appear only as rail Eng-card substrate facts — acceptable for entities outside the Prep-Assemble stage, named for completeness.

### RISK

- **RISK — double-clear leaves a false statement on the client surface.** `clearGate(2)` updates the row but not `extract-held-note`; clear gate 1 then gate 2 and the extract still reads "The remaining Unverified item stays held at the confirm-gate" while nothing is held [CS: HIGH — traced in code, not executed]. The cognitive walkthrough's own criterion is "can she tell whether it worked" — a tester who clears both gates gets a screen that lies to her.
- **RISK — gate 2 clears into nothing.** Its cleared text says "released — extract copy not seeded at Tier 1," which is honest, but a row that clears and never lands can read as the gate silently eating content during the 5-second test. Watch for it in results.
- **RISK — hardcoded "22/30" goes stale now.** Both the fidelity banner and the rail badge bake in "critic pass 1: 22/30"; this document makes that number wrong. Any hardcoded critic score in UI copy will always be one pass behind.
- **RISK — "44 voices waiting" is an unsourced count.** The Hard Mode stub introduces a specific number that appears nowhere in the brief [CS: UNKNOWN — may trace to council.md/intake; not verifiable from the reviewed docs]. The modal that was carefully stripped of invented tensions still carries one invented-or-untraced number.
- **RISK — judge demo fill vs. its own caption.** The fields say "only the human fills this," and one click inserts pre-authored copy. It is labeled "(demo)" and the attribution says "sim demo — this judgment is the human's, not the Coworker's," but a literal walkthrough participant may log "the tool wrote the judgment." Brief the test facilitator.
- **RISK — extract footer collapses the tier grammar.** The Benchmarked register row lands client-side as "verified against our records; comparison context estimated" — Benchmarked and Estimated become the same client word. Nielsen #4 (tiers render consistently) is the floor this brushes; the content audit should note whether the tier→client-language mapping is a designed translation or a drift.
- **RISK — design tokens diverge from the stated Apex convention on radius, with a claimed override.** Radius tokens are 2/4/16px against the 4/6/8px convention, and Manrope joins Inter/JetBrains Mono — both annotated "[CS: VERIFIED] live Figma pull, Apex 2.0, 2026-07-31." Critic cannot verify the pull [CS: UNKNOWN]. If the pull is real, the file is more correct than the convention; if not, it is off-token with a verification stamp on it. One person confirms the Figma pull happened; that closes it. Dark-mode and status palettes are self-labeled [CS: UNKNOWN] on-screen — honest, and correctly scoped.
- **RISK — ux.md footnote 59 is still uncorrected upstream.** The build renders the truth and the brief's precedence rule covers it, but pass-1 gap 4's actual fix ("correction applied separately") is not verifiable from the reviewed files. Until ux.md is amended, every future reader of the governing doc re-inherits the "tier already in code" error.
- **RISK — bare "MCPs" in the pre-connected chip.** "assumes N-able MCPs ship authed" uses the plural without the Server/Instance distinction U2 locks. It mirrors the source question's own phrasing (J. Scally), and the section note carries the correct grammar ("via an MCP Instance … never 'a singular MCP'"), so this is borderline — but it is the one place the locked vocabulary loosens.

### Pass-1 overclaim traps — closure table

| Trap (pass 1) | Status |
|---|---|
| Pre-connected N-able MCPs need on-screen [A] | **CLOSED** — chip on all four first-party cards |
| Footnote 59 "tier in code" contradiction | **CLOSED in build** (proposed-grammar label + grep receipt on-screen); ux.md correction itself unverified [CS: UNKNOWN] |
| "Under 20 minutes" seeding in from ux.md | **CLOSED** — absent [D — verified live 2026-07-31] |
| Value Ledger dollar figures | **CLOSED** — only the "$0" substrate quote, internal rail [D — verified live 2026-07-31] |
| Council modal's 7 invented tensions | **CLOSED** — pending slots, zero invented; residual: untraced "44 voices" count |
| Readiness score read as metric | **PARTIAL** — [A]-labeled in rail, but duplicated unlabeled in the fidelity banner (FAIL above) |

---

## What gates the handoff

Ordered; all are copy-level:

1. Remove the readiness line from the fidelity banner (or move the number rail-side only) — contract FAIL, sits above the client surface.
2. Stamp the 0.4× chip `SEED DATA — invented 2026-07-31`.
3. Fix the double-clear stale held-note in `clearGate(2)`.
4. Amend the inventory to list (or strip) the health snapshot and premise banner.
5. Source or remove "44 voices"; de-hardcode "22/30" or accept staleness knowingly.
6. Confirm the 2026-07-31 Apex Figma pull for radius/Manrope, and land the ux.md footnote-59 correction.

None of these block running the three Gate-3 tests; items 1–3 should land before test participants see the screen, because two of them put false or unlabeled numbers in front of the exact audience the tests measure.

---

## Orchestrator annotations (2026-07-31, post-review)

- Item 6's ux.md footnote-59 correction: **already landed** before this review ran (edits to ux.md §6 operating principle 2 + footnote 59, dated 2026-07-31) — critic could not see it because it reviewed the build, not ux.md's current state. Closed.
- "44 voices": sourced — the council is 44 methods across 14 clusters per `briefs/methods-library-matrix.md` and the prototype skill spec. Attribution added to the stub label in v2; not invented.
- Items 1–4: applied to v1 as copy-level edits (see .bradley-ledger) and relayed to the v2 build mid-flight as contract overrides.

## Honor check

- **Judgment held by:** Beth. Whether the three FAILs gate the handoff, whether the unlisted elements stay, and when the Gate-3 tests run are hers; this document names problems and stops. No fixes applied, no files modified by critic.
- **Mechanics performed:** full read of the built HTML against the brief's build contract and pass 1; behavior claims split between [D — verified live 2026-07-31] (orchestrator's browser pass, cited as given) and [CS: HIGH] (traced in code by critic, not executed).
- **[CS: VERIFIED]** — every quoted string, class, and token above was read directly from `prototypes/qbr-prep-sim/index.html` this pass.
- **[CS: UNKNOWN]** — the 2026-07-31 Apex Figma pull behind the radius/Manrope/brand annotations; the source of "44 voices"; whether ux.md footnote 59 has been corrected. Named at their punch-list items. (Two of three resolved in orchestrator annotations above.)
- **No validation claimed.** Tier 1 Concept, PROCEED-FLAGGED, carried unchanged. 26/30 is a build-compliance score, not evidence anyone wants this.
